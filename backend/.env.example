# Configuración del servidor
PORT=3000
NODE_ENV=production

# JWT Secret Key
JWT_SECRET=cremosos_secret_2024_change_this_in_production

# MongoDB Atlas Connection String
# Reemplaza <username>, <password> y <cluster> con tus datos de MongoDB Atlas
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/cremosos?retryWrites=true&w=majority

# Ejemplo:
# MONGODB_URI=mongodb+srv://admin:miPassword123@cluster0.abc123.mongodb.net/cremosos?retryWrites=true&w=majority

# URL base para el frontend (CORS)
FRONTEND_URL=http://localhost:8080
